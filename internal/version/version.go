// Package version exposes the orcha build version, shared by the binary's
// -version flag and the /api/health endpoint so both report the same value.
package version

// Version is the current orcha version.
const Version = "0.1.0"
